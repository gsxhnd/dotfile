# dotfile

1. 安装 git curl zsh
   1. ubuntu:    `apt install -y git curl zsh`
2. 安装 chezmoi
   1. `sh -c "$(curl -fsLS get.chezmoi.io)"`

3. 安装shell
   1. sh -c "$(curl -fsSL https://raw.githubusercontent.com/ohmyzsh/ohmyzsh/master/tools/install.sh)"

   2. sh -c "$(curl -fsSL https://install.ohmyz.sh)"
